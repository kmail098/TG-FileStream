{
  "name": "tgfs-bot",
  "version": "1.0.0",
  "scripts": {
    "build": "apt-get update && apt-get install -y ffmpeg && pip install -r requirements.txt",
    "start": "gunicorn tgfs.__main__:app"
  }
}
